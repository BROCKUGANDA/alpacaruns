# Secrets — never commit
.env
.env.local

# Go build output
bin/
*.exe
*.test
*.out

# Node (defensive — none used but harmless)
node_modules/

# OS
.DS_Store

# Runtime data (state files, trade logs)
data/
